package {{packageName}}

import {{entityPackageName}}.{{entityClassName}}
import java.time.LocalDateTime
import io.swagger.annotations.*
import java.util.*

/**
* @author afezeria
*/

class {{name}}(
{{#fields}}
    @ApiModelProperty(value = "{{comment}}", {{#notnull}}required = true{{/notnull}})
    var {{fieldName}}: {{javaType}}{{^notnull}}? = null{{/notnull}},
{{/fields}}
    @ApiModelProperty(value = "")
    var id: String? = null,
){
    fun toEntity(): {{entityClassName}} {
        return {{entityClassName}}(
      {{#fields}}
            {{fieldName}} = {{fieldName}},
      {{/fields}}
        )
    }

    fun fromEntity(entity: {{entityClassName}}): {{dtoClassName}} {
        return entity.run {
            {{dtoClassName}}(
            {{#fields}}
                {{fieldName}} = {{fieldName}},
            {{/fields}}
          )
        }
    }

    fun update(entity: {{entityClassName}}) {
        entity.also {
      {{#fields}}
            it.{{fieldName}} = {{fieldName}}
      {{/fields}}
        }
    }
}
